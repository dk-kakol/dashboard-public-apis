version: "3.4"
services:
 dashboard-public-apis:
   image: node:current
   container_name: dashboard-public-apis
   entrypoint: /bin/sh
   ports:
     - 8000:8000
   working_dir: /srv/app
   volumes:
     - type: bind
       source: ./
       target: /srv/app
   tty: true

