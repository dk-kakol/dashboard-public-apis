# v-orphans

The removeOrphans Vue directive is designed to enhance the readability of text within an HTML element by eliminating orphans, which are single words left alone at the end of a paragraph or line.

## Usage

```
<div v-orphans>Text with orphans</div>
```
