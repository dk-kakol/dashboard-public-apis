export enum ApiEndpoints {
  list = 'api-entries',
  resources = 'resources'
  // random = 'random',
  // categories = 'categories',
  // testError = 'test/:id'
}

export type EndpointParams = Record<string, any>;
